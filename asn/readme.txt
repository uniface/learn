This folder contains configuration files used by the IDE and Uniface middleware.

ide.asn is used by the IDE shortcut.
dbms.asn specifies the database connection, and included from ide.asn.
urouter.asn is included in your Uniface installation's common\adm\urouter.asn to configure a server for web development, which will use wasv.asn.

